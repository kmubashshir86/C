#include <stdio.h>
#include <math.h>
void main(void) {
    printf("enter base:");
    int b;
    scanf("%d",&b);
    printf("enter exp : ");
    int a;
    scanf("%d",&a);
    printf("%f",pow(b,a));//return floats 
    
}